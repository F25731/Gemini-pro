services:
  banana-pro-wrapper:
    build: .
    image: banana-pro-wrapper:latest
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      PORT: "3000"
    volumes:
      - ./data:/app/data
